# Optional runtime overrides for the Rust backend. Export these in your shell
# before launching the app; both have working defaults.

# Discovery scan auto-stop, in milliseconds.
DISCOVERY_TIMEOUT_MS=8000

# Per-host `/info` probe timeout, in milliseconds.
DEVICE_PROBE_TIMEOUT_MS=1500
